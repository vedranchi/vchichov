---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import PageHeader from '../components/PageHeader.astro';
import PostCard from '../components/PostCard.astro';
import { slugifyTag } from '../lib/format';

const posts = (
  await getCollection('blog', ({ data }) => (import.meta.env.PROD ? !data.draft : true))
).sort((a, b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());

// Unique tags for the little tag cloud.
const tags = [...new Set(posts.flatMap((p) => p.data.tags))].sort();
---

<BaseLayout title="Blog" description="Posts by Vedran Chichov on code, cycling, and life.">
  <section class="container blog">
    <PageHeader title="Blog">
      <Fragment slot="lede">Thoughts on code, riding, and whatever else I'm figuring out.</Fragment>
      {
        tags.length > 0 && (
          <ul class="tag-cloud chip-row">
            {tags.map((tag) => (
              <li>
                <a class="chip meta-label" href={`/blog/tags/${slugifyTag(tag)}`}>
                  #{tag}
                </a>
              </li>
            ))}
          </ul>
        )
      }
    </PageHeader>

    {
      posts.length > 0 ? (
        <ul class="grid">
          {posts.map((post) => (
            <li>
              <PostCard post={post} />
            </li>
          ))}
        </ul>
      ) : (
        <p class="body-text">No posts yet — the first one is on its way.</p>
      )
    }
  </section>
</BaseLayout>

<style>
  .blog {
    padding-block: var(--space-2xl);
  }
  .tag-cloud {
    margin-top: var(--space-sm);
  }
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-md);
    list-style: none;
    margin: 0;
    padding: 0;
  }
</style>
