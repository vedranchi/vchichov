// Collections are defined once in src/lib/collections.ts and shared by both sites, so the
// generated types at .astro/ are identical whichever build ran last. See that file.
export { collections } from '@shared/lib/collections';
