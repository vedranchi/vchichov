---
// Pixel-styled button. Renders as <a> when `href` is given, otherwise a <button>.
interface Props {
  href?: string;
  variant?: 'primary' | 'ghost';
  class?: string;
  type?: 'button' | 'submit';
}
const { href, variant = 'primary', class: className = '', type = 'button' } = Astro.props;
const Tag = href ? 'a' : 'button';
---

<Tag class:list={['pixel-btn', `is-${variant}`, className]} {...href ? { href } : { type }}>
  <slot />
</Tag>

<style>
  .pixel-btn {
    /* grid rather than inline-block so the label centres inside the 44px min touch target */
    display: inline-grid;
    place-items: center;
    min-height: 44px;
    font-family: var(--font-pixel);
    font-size: var(--text-xs);
    letter-spacing: var(--tracking-label);
    text-transform: uppercase;
    text-decoration: none;
    line-height: 1;
    padding: 0.7em 1.1em;
    cursor: pointer;
    border: 3px solid var(--frame-ui);
    border-radius: var(--radius);
    box-shadow: var(--pixel) var(--pixel) 0 0 var(--frame-ui);
    transition:
      transform 0.05s ease,
      box-shadow 0.05s ease,
      background-color 0.1s ease;
  }
  /* --accent-ui, not --accent: the lighter leaf only reaches 3.75:1 behind the label */
  .is-primary {
    background: var(--accent-ui);
    color: var(--on-accent);
  }
  .is-primary:hover {
    background: var(--accent-ui-hover);
    color: var(--on-accent);
  }
  .is-ghost {
    background: var(--surface);
    color: var(--text);
  }
  .is-ghost:hover {
    background: var(--surface-2);
    color: var(--text);
  }
  /* pressed-in effect: button slides into its shadow */
  .pixel-btn:active {
    transform: translate(var(--pixel), var(--pixel));
    box-shadow: 0 0 0 0 var(--frame-ui);
  }
</style>
