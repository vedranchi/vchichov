---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProjectCard from '../components/ProjectCard.astro';
import type { Project } from '../components/ProjectCard.astro';
import SocialLinks from '../components/SocialLinks.astro';
import { site } from '@shared/data/site';
import { home } from '@shared/data/home';
import projectData from '@shared/data/projects.json';

const all = projectData.projects as Project[];
// Featured order comes from home.ts; anything missing is skipped rather than erroring.
const projects = home.featured
  .map((name) => all.find((p) => p.name === name))
  .filter((p): p is Project => Boolean(p));
---

<BaseLayout>
  <section class="container hero">
    <h1 class="hero-name">{site.name}</h1>
    <p class="hero-role">Software engineer</p>

    <div class="hero-intro prose measure">
      <p>Hi, I'm Vedran — a first-year student in Skopje, North Macedonia.</p>
      <p>
        I build things and then keep them running. Right now that's <strong>Glucoread</strong>, a
        diabetes tracking app that's live and self-hosted. Away from the keyboard I race bikes for {
          site.team.name
        }.
      </p>
    </div>

    <SocialLinks />
  </section>

  <section class="container work" aria-labelledby="work-heading">
    <h2 id="work-heading" class="section-title">Work</h2>
    <div class="cards">
      {projects.map((project) => <ProjectCard project={project} />)}
    </div>
    <p class="more">
      <a class="inline" href="/projects">See everything I've built</a>
    </p>
  </section>
</BaseLayout>

<style>
  .hero {
    padding-block: clamp(2.5rem, 8vw, 4.5rem) clamp(2rem, 6vw, 3.5rem);
  }
  .hero-name {
    font-size: clamp(2.5rem, 8vw, 3.5rem);
    font-weight: 700;
    letter-spacing: -0.025em;
  }
  .hero-role {
    margin-top: 0.4rem;
    font-size: clamp(1.125rem, 3vw, 1.375rem);
    font-weight: 500;
    color: var(--text);
  }
  .hero-intro {
    margin-top: 2.25rem;
    display: grid;
    gap: 1.1rem;
  }
  .hero-intro strong {
    font-weight: 700;
  }
  /* The icon row sits well below the text, as in the reference. */
  :global(.hero .socials) {
    margin-top: 2.75rem;
  }

  .work {
    padding-bottom: 1rem;
  }
  .section-title {
    font-size: clamp(1.75rem, 4vw, 2.25rem);
    margin-bottom: 1.5rem;
  }
  .cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(100%, 17rem), 1fr));
    gap: 1.25rem;
  }
  .more {
    margin-top: 1.75rem;
  }
</style>
